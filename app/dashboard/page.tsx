import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{requireAuthoritativeUser}from"@/lib/auth/server";import{dashboardCards}from"@/lib/dashboard/metrics";import{formatINR}from"@/lib/locale/india";import{getDashboardMetrics}from"@/lib/supabase/queries"
export default async function DashboardPage(){const user=await requireAuthoritativeUser();const metrics=await getDashboardMetrics(user);const title={admin:"Admin Dashboard",branch_manager:"Branch Dashboard",teacher:"Teacher Dashboard",student:"Student Dashboard"}[user.role];const money=new Set(["Fees collected","Outstanding fees","Fees assigned"]);return <div className="space-y-6 p-6"><div><h1 className="text-3xl font-bold">{title}</h1><p className="mt-2 text-muted-foreground">Live institute data within your permitted scope</p></div><div className="grid grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-4">{dashboardCards(user.role,metrics).map(([label,value])=><Card key={label}><CardHeader className="pb-2"><CardTitle className="text-sm font-medium text-muted-foreground">{label}</CardTitle></CardHeader><CardContent><div className="text-3xl font-bold">{money.has(label)?formatINR(value):value.toLocaleString("en-IN")}</div></CardContent></Card>)}</div></div>}
